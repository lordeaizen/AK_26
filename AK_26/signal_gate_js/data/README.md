# Data module

The current prototype saves Stage 1 results to browser localStorage.

Key:
signalGate_stage1

Later you can replace this with:

- CSV export
- JSON export
- SQLite (Python backend)
- Firebase/Supabase
- your own API/database

Keep the database/export code separate from the stage files.
